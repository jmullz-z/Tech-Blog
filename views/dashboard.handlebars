<div class="mb-5 pt-3"
    <a href="/dashboard/create/"><button id="start-post" class="button is-primary is-pulled-right>start post<button></a>

    <h2 class="title pb-4">dashboard: all posts</h2>hr>
{{#if posts.legnth}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}

    <a href="/dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5 is-attention">edit post</button></a>
    </div>
    {{/each}}

   {{else}}
   <h6 class=""pb-4 has-text-centered is-bold">no new posts</h6> 

   {{/if}}